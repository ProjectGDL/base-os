---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: containerfile
snippets:
  - |
    RUN echo 'g input - -' > /usr/lib/sysusers.d/input.conf && \
        systemctl enable gdl-input-group.service
