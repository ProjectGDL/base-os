---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: containerfile
snippets:
  - |
    RUN dnf install -y python-pyside6 layer-shell-qt  && \
        echo 'import "/usr/share/ublue-os/just/60-throne.just"' >> /usr/share/ublue-os/justfile && \
        echo 'import "/usr/share/ublue-os/just/61-amneziavpn.just"' >> /usr/share/ublue-os/justfile && \
        echo 'import "/usr/share/ublue-os/just/62-v2rayn.just"' >> /usr/share/ublue-os/justfile && \
        echo 'import "/usr/share/ublue-os/just/63-tgwsproxy.just"' >> /usr/share/ublue-os/justfile && \
        echo 'import "/usr/share/ublue-os/just/64-inetresqued.just"' >> /usr/share/ublue-os/justfile && \
        echo 'import "/usr/share/ublue-os/just/65-zapret.just"' >> /usr/share/ublue-os/justfile && \
        echo 'import "/usr/share/ublue-os/just/66-warp.just"' >> /usr/share/ublue-os/justfile
