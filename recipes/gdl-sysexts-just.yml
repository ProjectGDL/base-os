---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: containerfile
snippets:
  - |
    RUN echo 'import "/usr/share/ublue-os/just/60-throne.just"' >> /usr/share/ublue-os/justfile && \
    RUN echo 'import "/usr/share/ublue-os/just/61-amneziavpn.just"' >> /usr/share/ublue-os/justfile
